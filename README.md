# Omnom Linux

## Default settings:
- Linux From Scratch base
- OpenRC init
- Hyprland Wayland desktop
- Fish shell
- Kitty terminal
- NixOS style generations
- Low RAM usage

# some commands what i want to add

omnom-install - looks for dnf,pacman,aur,OUR(omnom user repository) and if don't found trying compiling from source like gentoo or something like that also here's some usages omnom-install firefox --emerge (emerge = compile from source) omnom-install firefox --our omnom-install firefox --dnf omnom-install firefox --pacman omnom-install --our and here one more command omnom-upgrade it's just updating like you know here's some usages omnom-upgrade --all (update everything like sudo pacman -Suy) omnom-upgrade firefox
